<template>
  <div
    class="flex h-16 items-center border-b border-(--border-color) px-4"
    :class="isCollapsed ? 'justify-center' : 'justify-between'"
  >
    <div
      v-if="!isCollapsed"
      class="flex items-center gap-2 overflow-hidden whitespace-nowrap transition-all"
    >
      <slot>
        <span class="text-lg font-bold text-(--label-text)">Brand</span>
      </slot>
    </div>

    <div v-else>
      <slot name="icon">
        <div class="h-8 w-8 rounded-lg bg-primary" />
      </slot>
    </div>

    <button
      class="absolute -right-3 top-5 hidden md:flex h-6 w-6 items-center justify-center rounded-full border border-(--border-color) bg-(--surface-primary) text-(--icon-color) hover:bg-(--surface-secondary) transition-colors z-[100] shadow-sm cursor-pointer"
      @click="toggleCollapse"
    >
      <Icon :name="isCollapsed ? 'mdi:chevron-right' : 'mdi:chevron-left'" class="h-4 w-4" />
    </button>
  </div>
</template>

<script setup lang="ts">
import { useSidebar } from "~/composables/useSidebar";
const { isCollapsed, toggleCollapse } = useSidebar();
</script>
